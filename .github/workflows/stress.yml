name: Stress

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  stress:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    continue-on-error: true

    steps:
      - uses: actions/checkout@v4

      - name: Run docker stress suite
        run: make docker-stress
